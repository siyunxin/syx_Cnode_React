import React, { Component } from 'react'
import { Layout } from 'antd'
export default class CnodeFooter extends Component {
    render() {
        return (
            <Layout.Footer className="cnodeFooter">
                我是底
            </Layout.Footer>
        )
    }
}